name: VM2 dual check/status collision

on:
  pull_request:
    branches:
      - dual-collision-base

permissions:
  contents: read

jobs:
  gate:
    name: vm2-dual-collision-gate
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo VM2_DUAL_CHECKRUN_FAILURE
          exit 1
