name: VM2 classic App-bound producer control

on:
  pull_request:
    branches:
      - classic-app-bound-base

permissions:
  contents: read

jobs:
  fail-expected-producer:
    name: "Rule: VM2 classic app-bound producer control (comment)"
    runs-on: ubuntu-latest
    steps:
      - name: Expected GitHub Actions producer deliberately fails
        run: |
          echo VM2_CLASSIC_EXPECTED_APP_FAILURE
          exit 1
